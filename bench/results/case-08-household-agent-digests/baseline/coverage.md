# Requirement coverage

Legend: `covered` means the checked source documents the capability directly; `partial` means it covers a useful primitive but leaves a material gap; `missing` means no support was established in the checked source set. Privacy and consent rows describe what the source provides; a custom orchestration layer is still needed to enforce the household policy consistently.

| Requirement | Cozi | FamilyWall | Google stack | Microsoft stack | Apple stack | Todoist | What remains |
|---|---|---|---|---|---|---|---|
| Morning calendar briefing | covered via shared calendar and agenda emails | covered via shared calendar/reminders | covered via Calendar API + scheduler | covered via Outlook/Graph | partial: Shortcuts is device-specific | partial: calendar sync, no digest | Compose concise output and deliver to household recipients. |
| Morning chores/overdue tasks | covered via shared to-dos and recurring Chores | partial: to-dos documented, digest not | partial: Tasks API is per-user; shared repeating tasks gap | covered via shared To Do lists and recurrence | covered for shared Reminders lists, central digest gap | covered for shared projects/tasks | Define source of truth and recurrence semantics. |
| Weekly overdue + budget + maintenance + meeting-note action points | missing as one documented workflow | partial: budget tracker, no checked notes digest | missing as one product feature | missing as one product feature | missing as one product feature | missing as one product feature | Build the aggregator and adapters for budget/maintenance/notes. |
| Natural-language “next Thursday” command | partial: reminders/events exist; parser not documented | missing in checked docs | partial: `quickAdd` takes text; task choice/date disambiguation is custom | partial: Graph writes, parser is custom | covered for Siri reminders; central command still custom | covered for task dates | Resolve exact date/time zone and show a confirmation preview. |
| Create a real task or event | partial: internal Cozi objects; external feeds read-only | partial: internal objects and calendar sync | covered by Calendar/Tasks APIs | covered by Graph event/task APIs | covered in native apps/Shortcuts | covered for tasks; calendar is sync-oriented | Select task vs event and perform an idempotent write. |
| Per-person calendar visibility | missing: fully shared account, no restricted access | partial: private circle and selected calendar sharing | covered: ACL roles and private events | partial: shared-calendar permissions; cross-tenant caveats | partial: calendar read-only/edit, no checked item-level redaction | partial: shared projects expose all project information | Enforce source ACLs and recipient-specific redaction in the layer. |
| Per-person private notes/budget | missing in Cozi | partial: private circle, no checked field-level policy | partial: can use separate restricted Docs/Sheets, but custom adapter needed | partial: can use restricted Microsoft sources, adapter needed | missing in checked docs | missing in checked docs | Store scopes and classifications for every connector and field. |
| Ask before anything leaves household | missing as a native policy | missing as a native policy | partial: API can control notification mode, but layer must gate recipients | partial: Graph sends attendee invitations automatically | partial: Shortcuts can ask or run automatically, but no household policy | missing as a household policy | Block external recipients by default; ask at final action and audit consent. |

The user’s full specification is therefore not already covered by one existing stack. Calendar/task primitives are mature; the differentiating work is privacy-aware cross-source summarization, explicit recipient policy, and safe writes.
