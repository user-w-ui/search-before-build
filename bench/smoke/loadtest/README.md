# 2026-08-29 联通性测试存档（非正式配对臂）

这是一次**加载/运行/反问应答的联通性验证**，不是 `real/README.md` 协议下的正式 A/B 臂运行：
会话被中途截断过一次（超时），最后用"停止扩张、按技能流程收尾"的指令跑完，因此不进入 `results/<case-id>/` 的正式对比口径。

| 文件 | 说明 |
| --- | --- |
| brief.html | 插件渲染的临时 HTML 简报（源文件：`<temp>/search-before-build/latest.html`） |
| kernel-input.json | 收尾轮次的内核输入（8 个 retrieval envelope） |
| kernel-output.json | 内核输出（15 条归一化记录、5 个候选） |

会话事实（来自 Claude Code 会话文件时间戳，UTC）：
- 技能开始 04:07:22Z，检索前确认 04:10:15Z，会话最后一条 04:23:30Z（本地 +08:00 即 12:07–12:23，全程约 16 分钟）。
- 研究阶段在确认后运行约 4 分钟即被 240s 超时截断，并非数小时；收尾轮次约 6 分钟完成。
