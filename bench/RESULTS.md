# Offline benchmark

The current cases are smoke tests for the benchmark runner and fixture format. They are intentionally small and must not be presented as proof that Search Before Build outperforms ordinary Web search.

Run:

```bash
npm run benchmark:offline
```

Before publishing quality claims, add the frozen evaluation set, run the same model and tool budget for the baseline and pipeline groups, and commit a dated result snapshot under `bench/results/`.
